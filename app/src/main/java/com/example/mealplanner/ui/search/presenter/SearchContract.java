package com.example.mealplanner.ui.search.presenter;

public interface SearchContract {
    interface View {
        // Define view methods as needed
    }

    interface Presenter {
        // Define presenter methods as needed
    }
}
